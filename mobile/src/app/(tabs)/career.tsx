import { View, Text } from "react-native";

export default function Career() {
  return (
    <View className="flex-1 items-center justify-center bg-background p-6">
      <Text className="text-lg font-semibold text-foreground">My career roadmap</Text>
      <Text className="mt-1 text-center text-sm text-muted-foreground">Tiers, skill gaps and progress — arriving in BUILD_9.</Text>
    </View>
  );
}
