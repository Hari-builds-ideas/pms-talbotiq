# ─── Core runtime (pinned) ─────────────────────────────────────────────
# Stack is LOCKED per CLAUDE.md: Django 4.2 + DRF, MySQL 8, Redis 7 +
# Celery, simplejwt, django-allauth, django-otp, Argon2. Do not bump majors.
Django==4.2.16
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
PyJWT==2.9.0

# Auth: OAuth/OIDC (allauth) + SAML 2.0 SP (python3-saml) + MFA TOTP (django-otp)
# + Argon2 hashing. python3-saml is an SP toolkit (parse+validate assertion only),
# so SSO terminates in our own JWT issuance — see DECISIONS D25. It pulls xmlsec +
# lxml, which need the libxmlsec1/libxml2 system libs added in the Dockerfile.
django-allauth==0.63.6
python3-saml==1.16.0
django-otp==1.5.4
argon2-cffi==23.1.0
cryptography==43.0.1
requests==2.32.3

# Cache, sessions, queue/broker — Redis is the ONLY broker for MVP (no Kafka)
celery==5.4.0
redis==5.0.8
django-redis==5.4.0

# Health/readiness probes (/readyz)
django-health-check==3.18.3
# OpenAPI 3 schema generated from the real views + serializers (F6).
drf-spectacular==0.30.0

# Observability: error tracking (Sentry) + Celery monitoring (Flower)
sentry-sdk==2.18.0
flower==2.0.1

# Database driver (MySQL 8)
mysqlclient==2.2.4

# AI agent stack (Module 10 go-live). langgraph keeps the agent node-sequences on
# the locked StateGraph stack; langsmith enables optional tracing (no-op without a
# key). The Groq provider is OpenAI-compatible and uses `requests` (above) — no
# extra SDK. See apps/ai/groq.py + NEEDS_HARI_llm_provider.md.
langgraph==0.2.61
langsmith==0.1.147

# Object storage for uploaded media (C9). Only imported when USE_GCS_MEDIA is on
# — Django resolves STORAGES backends lazily by dotted path — so a deployment on
# the local-filesystem fallback never touches the Google client libraries.
django-storages[google]==1.14.4

# Config + ops
django-environ==0.11.2
python-json-logger==2.0.7
gunicorn==22.0.0
whitenoise==6.7.0
