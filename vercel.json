{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": "FREE Vercel deploy for the React SPA (V1_C / DEPLOY_DEMO.md). Built from the REPO ROOT (not a frontend/ root dir) because the SPA imports the sibling ../shared/src via the @shared alias. The /api rewrite PROXIES to the Render backend so the browser sees ONE origin — no CORS + no backend change needed. ⚠ Replace the destination host below with YOUR Render URL after the backend deploys (one edit). With the proxy in place, leave VITE_API_BASE_URL unset (the SPA defaults to same-origin /api).",
  "framework": null,
  "installCommand": "cd frontend && npm ci",
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://REPLACE-WITH-YOUR-RENDER-HOST.onrender.com/api/:path*"
    },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
